# private hosts go in ~/.ssh/config.local (see the .example). missing file
# is fine. keep this Include first: ssh takes the first value it finds per
# keyword, so anything below a matching Host block would lose.
Include ~/.ssh/config.local

Host *.local
  ForwardX11 yes
